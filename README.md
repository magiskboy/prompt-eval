# Prompt eval


```bash
$ export LITELLM_PROXY_API_BASE=
$ export LITELLM_PROXY_API_KEY=
$ docker compose up -d
```

Open web browser at http://localhost:4000/ui to interact with LLM
In other tab, visit http://localhost:3000 to eval values
